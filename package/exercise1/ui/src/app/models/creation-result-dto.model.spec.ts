import { CreationResultDto } from './creation-result-dto.model';

describe('CreationResultDto', () => {
  it('should create an instance', () => {
    expect(new CreationResultDto()).toBeTruthy();
  });
});
