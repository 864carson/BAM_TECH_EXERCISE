import { AstronautHistoryDto } from './astronaut-history-dto.model';

describe('AstronautHistoryDto', () => {
  it('should create an instance', () => {
    expect(new AstronautHistoryDto()).toBeTruthy();
  });
});
