import { AstronautListDto } from './astronaut-list-dto.model';

describe('AstronautListDto', () => {
  it('should create an instance', () => {
    expect(new AstronautListDto()).toBeTruthy();
  });
});
