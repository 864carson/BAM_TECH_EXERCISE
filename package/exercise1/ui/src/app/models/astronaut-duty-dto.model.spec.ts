import { AstronautDutyDto } from './astronaut-duty-dto.model';

describe('AstronautDutyDto', () => {
  it('should create an instance', () => {
    expect(new AstronautDutyDto()).toBeTruthy();
  });
});
